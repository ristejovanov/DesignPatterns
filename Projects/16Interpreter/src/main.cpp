// Interpretator
//Definition - component that processed structured text data.
//dose so it turning it into separate lexical tokens(lexing) and then interpreting 
//sequences of side tokens(parsing)
#include "HandMadeParser.h"

int main()
{
	testFunction();


	return 0;
}